<?xml version="1.0" encoding="utf-8"?>
<!-- The Google Play build: updates come from Play, so the in-app updater's installer permission goes
     (Play's policy for REQUEST_INSTALL_PACKAGES); Updates.open() sends the customer to the listing. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.REQUEST_INSTALL_PACKAGES"
        tools:node="remove" />

</manifest>
